<?php
/* @var $this \Magelight\Admin\Blocks\Scaffold\EntityList */
?>
<div class="page-header">
    <a class="btn btn-info" href="<?=$this->url('admin/scaffold/{entity}/create/', ['entity' => $this->_entity])?>"><?=('Create');?></a>
</div>

<div class="align-center">
    <?=$this->section('pager')?>
</div>
<table class="table">
    <thead>
    <tr>
        <?php foreach ($this->tableFields as $field) : ?>
            <th><?=$field?></th>
        <?php endforeach; ?>
            <th><?=__('Actions')?></th>
    </tr>
    </thead>
    <tbody>
        <?php foreach ($this->getCollection()->fetchModels() as $model) : ?>
            <tr>
                <?php foreach ($this->tableFields as $field) : ?>
                    <td><?=$model->$field?></td>
                <?php endforeach; ?>
                    <td>
                        <a class="btn btn-sm btn-info" href="<?=$this->url('admin/scaffold/{entity}/read/{id}', [$model->getIdField() => $model->getId(), 'entity' => $this->_entity])?>"><i class="glyphicon glyphicon-white icon-edit"></i></a>
                        <a class="btn btn-sm btn-danger" href="<?=$this->url('admin/scaffold/{entity}/delete/{id}', [$model->getIdField() => $model->getId(), 'entity' => $this->_entity])?>"><i class="glyphicon glyphicon-white icon-remove"></i></a>
                    </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<div class="align-center">
    <?=$this->section('pager')?>
</div>